# Hoja de ruta (borrador)

- [ ] Contratos Pydantic iniciales en `shared/contracts/`.
- [ ] Persistencia: esquema SQL y conexión desde ingest + compute.
- [ ] Endpoints de negocio en `backend-compute` (lecturas DuckDB + escrituras vía API si aplica).
- [ ] Autenticación entre portal/lab y API.
- [ ] Observabilidad (logs estructurados, trazas).
- [ ] Documentación de enlaces públicos para demos (sin acoplar marcas en el código).

## Integración con ecosistema externo

Cuando corresponda, documentar en README principal las URLs de la propiedad digital que enlazan a este hub (p. ej. sección de servicios o demos). El código permanece configurable vía entorno.
